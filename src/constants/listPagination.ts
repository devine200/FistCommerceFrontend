/** Rows fetched per API request and shown per table page across dashboard list views. */
export const DASHBOARD_LIST_PAGE_SIZE = 20

/** Maximum list limit accepted by admin list APIs. */
export const DASHBOARD_LIST_FETCH_LIMIT = 200
